---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ubuntu
    version: "16.04"

params:
  CONCOURSE_USER:
  CONCOURSE_PASS:
  CONCOURSE_HOST:

inputs:
  - name: busywork

run:
  path: busywork/ci/bootstrap/scripts/deploy.sh
