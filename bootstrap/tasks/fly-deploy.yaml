---
platform: linux

image_resource:
  type: docker-image
  source:
  source:
    repository: continuumio/miniconda3
    version: latest

params:
  CONCOURSE_USER:
  CONCOURSE_PASS:
  CONCOURSE_HOST:
  GITHUB_USER:
  GITHUB_PASS:
  FTP_URI:
  FTP_USER:
  FTP_PASS:
  ANACONDA_USER:
  ANACONDA_PASS:

inputs:
  - name: busywork
  - name: pipelines

outputs:
  - name: pipelines

run:
  path: busywork/bootstrap/tasks/fly-deploy.sh
